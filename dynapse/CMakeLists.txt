ADD_LIBRARY(dynapse SHARED dynapse.c)

SET_TARGET_PROPERTIES(dynapse
	PROPERTIES
	PREFIX "caer_"
)

TARGET_LINK_LIBRARIES(dynapse ${CAER_LIBS})

INSTALL(TARGETS dynapse DESTINATION ${CAER_MODULES_DIR})
